# level navigation
alias ..="cd .."
alias ...="cd ../.."
alias ....="cd ../../.."
alias home="cd ~"

# list
alias ll="ls -laG"
alias llt="ls -latrG"
alias lsize="du -a -h --max-depth=1 | sort -hr"

# reload
alias reload='source ~/.bash_profile'
alias reloadrc='source ~/.bashrc'